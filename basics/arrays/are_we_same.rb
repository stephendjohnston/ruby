array1 = [1, 5, 9]
array2 = [1, 9, 5]

puts array1 == array2

# false
# Note that == treats two Arrays as equal if they contain the same 
# number of elements, and each element in one Array is equal to the 
# corresponding element in the second Array.